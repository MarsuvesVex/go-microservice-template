server {
    listen 80;
    server_name _;

    location /healthz {
        proxy_pass http://api:3002/healthz;
    }

    location /api/ {
        rewrite ^/api/(.*)$ /v1/$1 break;
        proxy_pass http://api:3002;
    }
}
